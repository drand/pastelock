export type Ciphertext = {
    id: string
    createdAt: number
    decryptableAt: number
    ciphertext: string
}

export type Plaintext = {
    id: string
    createdAt: number
    decryptableAt: number
    plaintext: string
}